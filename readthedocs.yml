version: 2

sphinx:
  configuration: doc/source/conf.py

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  commands:
    - pip uninstall -y tlsql || true

python:
  install:
    - requirements: doc/requirements.txt
    - method: pip
      path: .

formats: []
