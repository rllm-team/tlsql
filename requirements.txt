# Core dependencies 
pytest>=7.4.0
pytest-cov>=4.1.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Database dependencies 
# For MySQL: pip install pymysql
# For SQLite: built-in (no extra package needed)

# Optional dependencies for BRIDGE model examples 
# rllm>=0.1.0  # Install separately: pip install rllm
# torch>=1.9.0  # Install separately: pip install torch


